# Server Settings
HOST=0.0.0.0
PORT=8000

# Database Settings
# Change these values for your local PostgreSQL instance
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/cancer_db

# Security & Authentication
# Generate a secure key in production (e.g. openssl rand -hex 32)
SECRET_KEY=medical_ai_platform_extremely_secure_secret_key_change_me_in_production_12345
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Directory and File Settings (Relative to backend/ or absolute paths)
MODEL_PATH=models/cancer_model.pth
UPLOAD_DIR=uploads
HEATMAP_DIR=heatmaps
REPORT_DIR=reports
